<% include ../partial/header %>
<div class="ui mamin container segment">
    <h1 class="ui header">New Comment to <%= book.title %></h1>
    <form class="ui form" action="/books/<%= book._id %>/comments" method="POST">
        <div class="field">
            <label>Comment</label>
            <input type="text" name="comment[com]" placeholder="Comment">
        </div>
        <div class="field">
            <label>writtenby</label>
            <input type="text" name="comment[writtenby]" placeholder="writtenby">
        </div>
        
        

        <button class="ui button" type="submit">Submit</button>
    </form>
</div>

<% include ../partial/footer %>